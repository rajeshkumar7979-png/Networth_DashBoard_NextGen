# Python bytecode / test artifacts
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Streamlit runtime state
.streamlit/secrets.toml

# Runtime data artifacts (regenerated by the app / scheduled jobs)
data/history.csv

# Intelligence Data Gateway cache (FRED/SEC disk fallback, deterministic keys)
data/intel_gateway_cache/

# Portfolio-aware Live Research cache (gnews query buckets, deterministic keys)
data/live_research_cache/
